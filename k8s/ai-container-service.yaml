# service.yaml
apiVersion: v1
kind: Service
metadata:
  name: ai-fastapi-service

spec:
  selector:
    app: ai-fastapi

  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000

  type: NodePort